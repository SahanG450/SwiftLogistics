services:
  # Infrastructure Services
  mongodb:
    image: mongo:7.0
    container_name: swiftlogistics-mongodb
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_ROOT_USERNAME: admin
      MONGO_INITDB_ROOT_PASSWORD: admin123
      MONGO_INITDB_DATABASE: swiftlogistics
    volumes:
      - mongodb_data:/data/db
    networks:
      - swiftlogistics-network
    healthcheck:
      test: echo 'db.runCommand("ping").ok' | mongosh localhost:27017/test --quiet
      interval: 10s
      timeout: 5s
      retries: 5

  rabbitmq:
    image: rabbitmq:3.12-management-alpine
    container_name: swiftlogistics-rabbitmq
    ports:
      - "5672:5672" # AMQP protocol
      - "15672:15672" # Management UI
    environment:
      RABBITMQ_DEFAULT_USER: admin
      RABBITMQ_DEFAULT_PASS: admin123
    volumes:
      - rabbitmq_data:/var/lib/rabbitmq
    networks:
      - swiftlogistics-network
    healthcheck:
      test: rabbitmq-diagnostics -q ping
      interval: 10s
      timeout: 5s
      retries: 5

  # Mock Systems (Python FastAPI Services)
  cms-mock:
    build:
      context: ./services/mocks/cms-mock
      dockerfile: Dockerfile
    container_name: swiftlogistics-cms-mock
    ports:
      - "3001:3001"
    volumes:
      - ./services/mocks/cms-mock/data:/app/data
    networks:
      - swiftlogistics-network
    environment:
      APP_NAME: "CMS Mock Service"
      HOST: 0.0.0.0
      PORT: 3001
      DEBUG: "true"
      CORS_ORIGINS: "http://localhost:3000,http://localhost:5173"
    healthcheck:
      test: [ "CMD", "curl", "-f", "http://localhost:3001/health" ]
      interval: 30s
      timeout: 10s
      retries: 3

  wms-mock:
    build:
      context: ./services/mocks/wms-mock
      dockerfile: Dockerfile
    container_name: swiftlogistics-wms-mock
    ports:
      - "3002:3002"
    volumes:
      - ./services/mocks/wms-mock/data:/app/data
    networks:
      - swiftlogistics-network
    environment:
      APP_NAME: "WMS Mock Service"
      HOST: 0.0.0.0
      PORT: 3002
      DEBUG: "true"
      CORS_ORIGINS: "http://localhost:3000,http://localhost:5173"
    healthcheck:
      test: [ "CMD", "curl", "-f", "http://localhost:3002/health" ]
      interval: 30s
      timeout: 10s
      retries: 3

  ros-mock:
    build:
      context: ./services/mocks/ros-mock
      dockerfile: Dockerfile
    container_name: swiftlogistics-ros-mock
    ports:
      - "3003:3003"
    volumes:
      - ./services/mocks/ros-mock/data:/app/data
    networks:
      - swiftlogistics-network
    environment:
      APP_NAME: "ROS Mock Service"
      HOST: 0.0.0.0
      PORT: 3003
      DEBUG: "true"
      CORS_ORIGINS: "http://localhost:3000,http://localhost:5173"
    healthcheck:
      test: [ "CMD", "curl", "-f", "http://localhost:3003/health" ]
      interval: 30s
      timeout: 10s
      retries: 3

  # Core Services (Python FastAPI)
  orchestrator:
    build:
      context: ./services
      dockerfile: orchestrator/Dockerfile
    container_name: swiftlogistics-orchestrator
    ports:
      - "4000:4000"
    depends_on:
      mongodb:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
    environment:
      MONGODB_URI: mongodb://admin:admin123@mongodb:27017/swiftlogistics?authSource=admin
      RABBITMQ_URL: amqp://admin:admin123@rabbitmq:5672
      JWT_SECRET: your-secret-key-change-in-production
      LOG_LEVEL: INFO
    networks:
      - swiftlogistics-network
    restart: on-failure

  api-gateway:
    build:
      context: ./services
      dockerfile: api-gateway/Dockerfile
    container_name: swiftlogistics-api-gateway
    ports:
      - "3000:3000"
    depends_on:
      - orchestrator
      - cms-mock
    environment:
      ORCHESTRATOR_URL: http://orchestrator:4000
      CMS_API_URL: http://cms-mock:3001
      JWT_SECRET: your-secret-key-change-in-production
      WEB_CLIENT_URL: http://localhost:5173
      LOG_LEVEL: INFO
    networks:
      - swiftlogistics-network
    restart: on-failure

  notification-service:
    build:
      context: ./services
      dockerfile: notification-service/Dockerfile
    container_name: swiftlogistics-notification-service
    ports:
      - "3004:3004"
    depends_on:
      rabbitmq:
        condition: service_healthy
    environment:
      RABBITMQ_URL: amqp://admin:admin123@rabbitmq:5672
      WEB_CLIENT_URL: http://localhost:5173
      LOG_LEVEL: INFO
    networks:
      - swiftlogistics-network
    restart: on-failure

  # Adapters (Python FastAPI)
  cms-adapter:
    build:
      context: ./services
      dockerfile: adapters/cms-adapter/Dockerfile
    container_name: swiftlogistics-cms-adapter
    ports:
      - "3005:3005"
    depends_on:
      rabbitmq:
        condition: service_healthy
      cms-mock:
        condition: service_started
      orchestrator:
        condition: service_started
    environment:
      RABBITMQ_URL: amqp://admin:admin123@rabbitmq:5672
      CMS_API_URL: http://cms-mock:3001
      LOG_LEVEL: INFO
    networks:
      - swiftlogistics-network
    restart: on-failure

  ros-adapter:
    build:
      context: ./services
      dockerfile: adapters/ros-adapter/Dockerfile
    container_name: swiftlogistics-ros-adapter
    ports:
      - "3006:3006"
    depends_on:
      rabbitmq:
        condition: service_healthy
      ros-mock:
        condition: service_started
      orchestrator:
        condition: service_started
    environment:
      RABBITMQ_URL: amqp://admin:admin123@rabbitmq:5672
      ROS_API_URL: http://ros-mock:3003
      LOG_LEVEL: INFO
    networks:
      - swiftlogistics-network
    restart: on-failure

  wms-adapter:
    build:
      context: ./services
      dockerfile: adapters/wms-adapter/Dockerfile
    container_name: swiftlogistics-wms-adapter
    ports:
      - "3007:3007"
    depends_on:
      rabbitmq:
        condition: service_healthy
      wms-mock:
        condition: service_started
      orchestrator:
        condition: service_started
    environment:
      RABBITMQ_URL: amqp://admin:admin123@rabbitmq:5672
      WMS_API_URL: http://wms-mock:3002
      LOG_LEVEL: INFO
    networks:
      - swiftlogistics-network
    restart: on-failure

networks:
  swiftlogistics-network:
    driver: bridge
    name: swiftlogistics-network

volumes:
  mongodb_data:
    name: swiftlogistics-mongodb-data
  rabbitmq_data:
    name: swiftlogistics-rabbitmq-data
